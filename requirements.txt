# CodeSage dependencies
# Install with: pip install -r requirements.txt

# LLM — Groq API client (free tier, no credit card)
groq>=0.9.0

# Embeddings — runs fully locally, no API needed
sentence-transformers>=3.0.0

# Vector search — fast local similarity search
faiss-cpu>=1.8.0

# Code parsing — understands function/class boundaries
tree-sitter>=0.23.0
tree-sitter-python>=0.23.0
tree-sitter-javascript>=0.23.0