# Copy to .env and fill in your own values (or set in Vercel's dashboard
# as a build-time env var). Never commit .env itself.

# Backend API URL. Defaults to http://localhost:8000 if unset, which is
# fine for local dev. Set this to your deployed Render URL once it's live.
VITE_API_URL=http://localhost:8000
